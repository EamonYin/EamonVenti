server:
  port: 8092
spring:
  application:
    name: storage
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/eamon_shopping?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true&useSSL=false
    username: root
    password:
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.217.128:8848
    alibaba:
      seata:
        tx-service-group: storage-service-fescar-service-group
mybatis-plus:
  global-config:
    db-config:
      insert-strategy: not_null
      update-strategy: not_null
      id-type: auto